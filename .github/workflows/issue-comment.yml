name: Disabled Legacy Issue Comment Bot

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled. Use .github/workflows/issue-comment-bot.yml."
